
// let divide = (param1, param2) => param1/param2
// divide(2000, 100)

// let square = (param, param2)

// let add = (param1, param2)

let divide = () => {
    return 2000 / 100
}

let square = num => { 
    return num * num
}


let add = (param1, param2) => {
    return param1 + param2
}